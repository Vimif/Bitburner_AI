# Bitburner AI Data Directory

Ce dossier contient les données persistantes de l'IA:

- `optimizer-config.txt` - Configuration optimisée (générée par l'optimizer)
- `optimizer-data.txt` - Historique de performance et statistiques des cibles

Ces fichiers sont créés et mis à jour automatiquement par le daemon optimizer.
